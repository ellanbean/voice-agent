build:
  docker:
    web: Dockerfile
    worker: Dockerfile
    dialer: Dockerfile
    interpreter: Dockerfile
run:
  web: uvicorn web.app:app --host 0.0.0.0 --port $PORT
  worker: python agent.py start
  dialer: python dialer.py
  interpreter: python interpreter.py start
